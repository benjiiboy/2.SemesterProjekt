<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_2.SemesterProjekt.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:_2.SemesterProjekt.Converter"
    xmlns:Viewmodel="using:_2.SemesterProjekt.Viewmodel"
    x:Class="_2.SemesterProjekt.View.RedigerBarn"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:IntToString x:Name="IntToString"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">





        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
            <TextBlock>
                <Run Text="Ændre fornavn:"/>
            </TextBlock>
            <TextBox x:Name="OpretbarnFornavn"/>
            <TextBlock>
                <Run Text="Ændre efternavn:"/>
            </TextBlock>
            <TextBox x:Name="OpretBarnEfternavn" />
            <TextBlock>
                <Run Text="Ændre Fødselsdag:"/>
            </TextBlock>
            <TextBox x:Name="OpretbarnFødselsdag" PlaceholderText="YYYYDDMM"/>
            <Button Margin="0,5,0,0" Content="Rediger"/>
        </StackPanel>

    </Grid>
</Page>
