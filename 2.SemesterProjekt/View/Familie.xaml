<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_2.SemesterProjekt.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Viewmodel="using:_2.SemesterProjekt.Viewmodel"
    x:Class="_2.SemesterProjekt.View.Familie"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" >
            <TextBlock FontSize="28" Margin="0,40,0,0" HorizontalAlignment="Center">
                <Run Text="Børn"/>
            </TextBlock>
            <ListView x:Name="ListViewBørn"
                      BorderBrush="Black"
                      BorderThickness="0.5"
                      Height="350"
                      Width="250">
                <ListView.DataContext>
                    <Viewmodel:VaccAppViewModel/>
                </ListView.DataContext>
            </ListView>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="opretBarn" Click="opretBarn_Click"
                        Content="Opret"
                        />
                <Button x:Name="SletBarn" Margin="50,0,0,0" Content="Slet"/>
            </StackPanel>
            <Button HorizontalAlignment="Center" Margin="0,20,0,0" Content="Se vaccinationskort"/>
        </StackPanel>
    </Grid>
</Page>
